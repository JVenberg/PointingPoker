{
  "name": "pointing-poker",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify && cp ./src/index.html ./src/app.js ./src/config.js ./dist/",
    "dev": "npm run build && firebase emulators:start",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^15.1.0",
    "tailwindcss": "^3.4.0"
  }
}
